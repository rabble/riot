<h1>New calendar</h1>

<% form_for(@calendar) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => "form_fields", :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', calendars_path %>

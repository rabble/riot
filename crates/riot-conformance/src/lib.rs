//! Conformance fixtures, hostile corpus, and deterministic test providers.
//! Deterministic clock/ID/signer implementations live here or under
//! `cfg(test)` only; `riot-ffi` release builds must never select them.

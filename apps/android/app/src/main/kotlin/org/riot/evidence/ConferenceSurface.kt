package org.riot.evidence

enum class ConferenceSurface(val label: String) {
    SPACES("Spaces"),
    INCIDENT_BOARD("Incident board"),
    COMPOSE_AND_SIGN("Compose & sign"),
    IMPORT_PREVIEW("Import preview"),
    CONNECTION("Connection"),
}
